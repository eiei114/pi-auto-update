# Contributing

## Development

```bash
npm ci
npm run ci
```

Test the extension locally with:

```bash
pi --no-extensions -e ./extensions/auto-update.ts
```

Keep changes focused. Add tests for behavior changes. Do not add npm tokens to repository secrets or workflows.
